#Usa `insert()` para agregar el número 0 al inicio de la lista y muestra la lista modificada

numeros=[numero for numero in range(1,6)]

numeros.insert(0,0) # el primer elemento es la posicion

print(numeros)